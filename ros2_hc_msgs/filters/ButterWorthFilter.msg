std_msgs/Header header

float32 cutoff_frequency     # Hz
uint8 order                  # Filter order
float32 sampling_rate        # Hz
bool zero_phase              # True if using filtfilt-style bidirectional filter
